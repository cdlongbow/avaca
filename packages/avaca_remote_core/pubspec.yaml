name: avaca_remote_core
description: MsQuic transport and range-resource core shared by Server and AVACA.
publish_to: none

environment:
  sdk: ^3.11.5
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter
  avaca_domain:
    path: ../avaca_domain
  avaca_protocol:
    path: ../avaca_protocol
  ffi: ^2.2.0

dev_dependencies:
  test: ^1.26.3
