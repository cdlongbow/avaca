name: avaca_scraper
description: Server-owned scrape and identity boundary for AVACA.
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  avaca_domain:
    path: ../avaca_domain
  html: ^0.15.6
  http: ^1.6.0

dev_dependencies:
  test: ^1.26.3
